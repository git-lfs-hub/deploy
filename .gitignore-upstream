# Downstream-only files: don't commit upstream, good to commit in forks.
# `config/upstream.sh setup` points core.excludesFile here and installs a
# pre-commit hook; the check-can-merge gate enforces it in CI when GLH_UPSTREAM
# is set.
vars.input.json
