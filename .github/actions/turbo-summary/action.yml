name: "Turbo Summary"
description: "Capture Turbo summary artifacts"
runs:
  using: "composite"
  steps:
    - uses: charpeni/turborepo-summary-action@v1
    # - name: Upload turbo summary artifacts
    #   uses: actions/upload-artifact@v7
    #   with:
    #     path: .turbo/runs/*.json
